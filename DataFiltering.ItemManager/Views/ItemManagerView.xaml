<UserControl x:Class="DataFiltering.ItemManager.Views.ItemManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataFiltering.ItemManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
    <Grid>
        <!--<ContentControl prism:RegionManager.RegionName="{Binding ItemManagementRegion}"/>-->
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Title}" FontSize="18"/>
                <local:ItemListView ItemsSource="{Binding Groceries, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Get" Command="{Binding GetGroceriesCommand}" Width="100"/>
            </StackPanel>
    </Grid>
</UserControl>
