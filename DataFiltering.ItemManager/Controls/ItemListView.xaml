<UserControl x:Class="DataFiltering.ItemManager.Controls.ItemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DataFiltering.ItemManager.Controls"
             mc:Ignorable="d">
    
    <StackPanel Orientation="Vertical" Width="150" Height="300" DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}">
        <TextBox Text="{Binding FilterText,UpdateSourceTrigger=PropertyChanged}"/>
        <ListBox Width="150" Height="300" ItemsSource="{Binding ItemsSource}" DisplayMemberPath="{Binding DisplayMemberPath}"/>
    </StackPanel>
</UserControl>
