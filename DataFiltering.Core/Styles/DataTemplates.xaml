<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <DataTemplate x:Key="GroceryItemTemplate">
        <materialDesign:Card Width="200" Height="50" Padding="5" Background="GreenYellow">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="{Binding ProductName}" Style="{StaticResource MaterialDesignTitleMediumTextBlock}"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="QTY: "/>
                        <TextBlock Text="{Binding StockQuantity}"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock  Grid.Column="1" Text="{Binding Price}" Style="{StaticResource MaterialDesignTitleLargeTextBlock}" VerticalAlignment="Center" TextAlignment="Right"/>
            </Grid>
        </materialDesign:Card>

    </DataTemplate>
</ResourceDictionary>